---
story_key: 1-3-saisir-une-prise
status: review
baseline_commit: NO_VCS
---

# Story 1.3 : Saisir une prise avec horodatage et contexte

## Story

**As a** utilisateur,
**I want** enregistrer une prise — alimentaire ou non — en quelques secondes,
**So that** je capture le moment exact sans effort de saisie (FR1, FR2, FR3, FR4, FR6).

## Acceptance Criteria

- **AC1** — À l'ouverture de l'application, je suis directement sur le formulaire de saisie, sans écran intermédiaire.
- **AC2** — La date/heure courantes sont pré-remplies et modifiables, y compris vers le passé.
- **AC3** — Je choisis une condition de prise parmi les 5 valeurs fixes (Chez moi, Au bureau, Au resto (business), Au resto (amis), Chez des gens) — champ obligatoire.
- **AC4** — La description, le poids estimé et les calories estimées sont tous facultatifs.
- **AC5** — Je peux ajouter une note libre sans limite de longueur perceptible.
- **AC6** — Je peux enregistrer une entrée ne contenant que l'horodatage, la condition et une note, sans erreur de validation.
- **AC7** — La table `entries` (horodatage, description, poids, calories, statut d'estimation, condition, note) est créée par migration Drizzle à cette étape.

## Tasks / Subtasks

- [x] **T1** — Schéma Drizzle `entries`
  - [x] T1.1 — Définir la table `entries` dans `src/server/db/schema.ts`
  - [x] T1.2 — Exporter `ENTRY_CONDITIONS`, `ENTRY_CONDITION_LABELS`, `ESTIMATION_STATUSES` pour partage Zod/UI
  - [x] T1.3 — Ajouter `casing: 'snake_case'` au `drizzle()` dans `src/server/db/index.ts`
  - [x] T1.4 — Générer la migration SQL avec `pnpm db:generate` → `drizzle/0000_hesitant_eternity.sql`

- [x] **T2** — Routeur tRPC `entries.create`
  - [x] T2.1 — Définir et exporter `createEntrySchema` (Zod, partagé UI/tRPC)
  - [x] T2.2 — Implémenter `entries.create` (insert Drizzle + `.returning()`)

- [x] **T3** — Composant `EntryForm`
  - [x] T3.1 — Créer `src/components/features/entry-form/EntryForm.tsx`
  - [x] T3.2 — Pré-remplir timestamp avec la date/heure courante (`datetime-local`)
  - [x] T3.3 — Réinitialiser le formulaire après enregistrement

- [x] **T4** — Page d'accueil
  - [x] T4.1 — `src/app/page.tsx` utilise `EntryForm` directement

- [x] **T5** — Validation du build
  - [x] T5.1 — `pnpm typecheck` : ✅ 0 erreur TypeScript
  - [x] T5.2 — `pnpm build` : ✅ 8 routes, 0 erreur

## Dev Notes

### Architecture

- Schémas Zod partagés entre tRPC et le formulaire — `createEntrySchema` exporté depuis `routers/entries.ts`
- Validation uniquement à la soumission (NFR4) — pas de validation à la frappe
- Indicateur de chargement discret sur le bouton submit (NFR4) — pas de loader plein écran
- Aucun framework de test pour le MVP — validation via TypeScript strict + `pnpm build`
- `casing: 'snake_case'` configuré dans `drizzle()` ET `drizzle.config.ts` (runtime + migrations)

### Table `entries`

```sql
CREATE TABLE "pacal_entry" (
  "id" integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  "timestamp" timestamp with time zone NOT NULL,
  "description" text,
  "weight_g" real,
  "calories" real,
  "estimation_status" text DEFAULT 'estime' NOT NULL,
  "condition" text NOT NULL,
  "note" text,
  "created_at" timestamp with time zone NOT NULL,
  "updated_at" timestamp with time zone
);
CREATE INDEX "entry_timestamp_idx" ON "pacal_entry" USING btree ("timestamp");
```

### 5 valeurs de condition (PRD FR-4)

| Code DB | Label UI |
|---------|----------|
| chez_moi | Chez moi |
| au_bureau | Au bureau |
| au_resto_business | Au resto (business) |
| au_resto_amis | Au resto (amis) |
| chez_des_gens | Chez des gens |

## Dev Agent Record

### Implementation Plan

1. Table `entries` dans `schema.ts` — colonnes en camelCase TypeScript → snake_case SQL via `casing`
2. `drizzle.config.ts` : ajout de `casing: 'snake_case'` pour que les migrations soient snake_case (important : sans ça, drizzle-kit génère du camelCase en SQL)
3. Routeur `entries.create` avec `createEntrySchema` Zod exporté
4. `EntryForm.tsx` : composant client avec état contrôlé, reset après succès
5. `page.tsx` : serveur (no `"use client"`) qui importe `EntryForm`

### Debug Log

- **Migration initiale en camelCase** : `drizzle()` avec `casing: 'snake_case'` est une option runtime, pas de build. Pour que drizzle-kit génère du snake_case dans le SQL, il faut aussi `casing: 'snake_case'` dans `drizzle.config.ts`. Fix appliqué, migration régénérée.

### Completion Notes

- ✅ Table `entries` définie avec 10 colonnes, migration SQL générée (`drizzle/0000_hesitant_eternity.sql`)
- ✅ `createEntrySchema` Zod exporté — source unique de validation partagée tRPC/form
- ✅ `entries.create` tRPC avec retour de l'entrée créée (`.returning()`)
- ✅ `EntryForm` : formulaire complet, pré-remplissage datetime, reset après succès, 5 conditions, champs optionnels
- ✅ Condition obligatoire (select, bouton désactivé si vide), tous les autres champs optionnels (AC6 satisfait)
- ✅ TypeScript strict sans erreur, build production réussi
- ⚠️ Pas de tests automatisés (décision architecture MVP explicite)
- ⚠️ `photo_path` absent à ce stade — sera ajouté en Story 1.4 par migration dédiée

## File List

### Nouveaux fichiers
- `pacal/src/components/features/entry-form/EntryForm.tsx`
- `pacal/drizzle/0000_hesitant_eternity.sql`
- `pacal/drizzle/meta/0000_snapshot.json`
- `pacal/drizzle/meta/_journal.json`

### Fichiers modifiés
- `pacal/src/server/db/schema.ts` (table entries + constantes)
- `pacal/src/server/db/index.ts` (casing snake_case)
- `pacal/src/server/api/routers/entries.ts` (create procedure + Zod schema)
- `pacal/src/app/page.tsx` (formulaire EntryForm)
- `pacal/drizzle.config.ts` (casing snake_case)

## Change Log

- **2026-06-19** — Story 1.3 implémentée : schéma Drizzle `entries`, migration SQL, tRPC `entries.create`, formulaire `EntryForm`, page d'accueil — build ✅

## Status

review
